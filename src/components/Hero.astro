<section class="hero" id="hero">
  <div class="hero__inner">
    <div class="hero__logo reveal" role="img" aria-label="Cris Vitral logo">
      <img
        src="/logo.webp"
        alt="Cris Vitral — Arte en cristal, hecho a mano"
        width="380"
        height="auto"
        style="display:block;width:100%;max-width:380px;height:auto;"
      />
      <span class="hero__logo-stamp">
        <strong>est.</strong>
        2010
      </span>
    </div>

    <h1 class="hero__tagline reveal" data-delay="1">
      Vitrales únicos, <em>hechos en casa</em>,<br/>con cariño.
    </h1>
    <p class="hero__lede reveal" data-delay="2">
      Soy Cris. Llevo más de quince años cortando cristal en mi taller.
      Cada pieza es única, hecha a mano de principio a fin, y casi todas
      cuestan menos de 100 €.
    </p>
    <div class="hero__actions reveal" data-delay="3">
      <a href="#gallery" class="btn btn--primary">
        Ver las piezas
        <svg class="btn__arrow" viewBox="0 0 14 10" aria-hidden="true">
          <path d="M1 5 L13 5 M9 1 L13 5 L9 9" />
        </svg>
      </a>
      <a href="#contact" class="btn btn--ghost">Hacer un encargo</a>
    </div>

    <div class="hero__handwriting reveal" data-delay="3">
      <svg width="40" height="20" viewBox="0 0 40 20" fill="none" aria-hidden="true">
        <path d="M2 15 Q 12 2, 22 12 T 38 8" stroke="#8B6340" stroke-width="1.5" stroke-linecap="round" />
        <path d="M34 4 L38 8 L34 12" stroke="#8B6340" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" fill="none" />
      </svg>
      envíos a toda España
    </div>
  </div>
</section>

<style is:global>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 8rem var(--gutter) 5rem;
    position: relative;
    overflow: hidden;
  }
  .hero::before {
    content: '';
    position: absolute;
    width: 280px; height: 280px;
    top: 15%; left: -80px;
    background: radial-gradient(circle, rgba(139, 99, 64, 0.08), transparent 70%);
    border-radius: 50%;
    pointer-events: none;
  }
  .hero::after {
    content: '';
    position: absolute;
    width: 320px; height: 320px;
    bottom: 10%; right: -100px;
    background: radial-gradient(circle, rgba(59, 110, 31, 0.06), transparent 70%);
    border-radius: 50%;
    pointer-events: none;
  }
  .hero__inner {
    text-align: center;
    max-width: 720px;
    position: relative;
    z-index: 2;
  }
  .hero__logo {
    display: inline-block;
    padding: 1.25rem 1.25rem 2.5rem;
    background: var(--color-paper);
    box-shadow:
      0 1px 0 rgba(58, 46, 26, 0.04),
      0 18px 30px -16px rgba(58, 46, 26, 0.2);
    transform: rotate(-1.2deg);
    margin-bottom: 3rem;
    position: relative;
  }
  .hero__logo::before {
    content: '';
    position: absolute;
    top: -14px;
    left: 50%;
    transform: translateX(-50%) rotate(-3deg);
    width: 90px; height: 26px;
    background: rgba(200, 146, 58, 0.4);
    border-left: 1px dashed rgba(200, 146, 58, 0.6);
    border-right: 1px dashed rgba(200, 146, 58, 0.6);
  }
  .hero__logo-stamp {
    position: absolute;
    bottom: -10px;
    right: -10px;
    width: 70px; height: 70px;
    border-radius: 50%;
    background: transparent;
    border: 1.5px solid var(--color-green);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    transform: rotate(8deg);
    font-family: var(--font-type);
    font-size: 0.55rem;
    color: var(--color-green);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    line-height: 1.2;
    text-align: center;
    opacity: 0.85;
  }
  .hero__logo-stamp strong {
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 500;
    font-size: 0.85rem;
    color: var(--color-green-deep);
    letter-spacing: 0;
  }
  .hero__tagline {
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 500;
    font-size: clamp(1.75rem, 3.5vw, 2.75rem);
    line-height: 1.25;
    color: var(--color-ink);
    margin: 0 0 1.25rem;
    text-wrap: balance;
  }
  .hero__tagline em {
    font-style: italic;
    color: var(--color-green);
  }
  .hero__lede {
    font-size: 1.05rem;
    color: var(--color-ink-soft);
    max-width: 460px;
    margin: 0 auto 2.5rem;
    line-height: 1.7;
  }
  .hero__actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }
  .hero__handwriting {
    font-family: var(--font-hand);
    font-size: 1.5rem;
    color: var(--color-brown);
    margin-top: 2.5rem;
    transform: rotate(-1.5deg);
    display: inline-block;
  }
  .hero__handwriting svg {
    display: inline-block;
    vertical-align: middle;
    margin-right: 0.4rem;
  }
</style>
