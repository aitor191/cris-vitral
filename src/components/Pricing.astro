<section class="pricing" id="pricing">
  <div class="container">
    <header class="section__head reveal">
      <span class="eyebrow">Precios claros</span>
      <h2 class="section__title">Sin sorpresas, <em>sin rodeos</em>.</h2>
      <p class="section__intro">
        Tres rangos según el tamaño. Te paso un boceto y un precio
        cerrado antes de empezar. Si te encaja, empezamos.
      </p>
    </header>

    <div class="price-tags">
      <article class="tag reveal">
        <div class="tag__label">Pequeñito</div>
        <h3 class="tag__title">Para colgar</h3>
        <div class="tag__price">desde 25 €</div>
        <div class="tag__price-from">— Suncatchers · detalles</div>
        <div class="tag__divider"></div>
        <ul class="tag__list">
          <li>Hasta 15 × 15 cm</li>
          <li>Entrega en 1–2 sem.</li>
          <li>Envío incluido</li>
        </ul>
      </article>

      <article class="tag reveal" data-delay="1">
        <div class="tag__label">Mediano</div>
        <h3 class="tag__title">Para ventana</h3>
        <div class="tag__price">desde 55 €</div>
        <div class="tag__price-from">— Cuadros · paneles</div>
        <div class="tag__divider"></div>
        <ul class="tag__list">
          <li>Hasta 40 × 40 cm</li>
          <li>Boceto previo incl.</li>
          <li>Entrega en 3–4 sem.</li>
        </ul>
      </article>

      <article class="tag reveal" data-delay="2">
        <div class="tag__label">Grande</div>
        <h3 class="tag__title">Para presumir</h3>
        <div class="tag__price">desde 90 €</div>
        <div class="tag__price-from">— Pieza especial</div>
        <div class="tag__divider"></div>
        <ul class="tag__list">
          <li>Hasta 70 × 70 cm</li>
          <li>Diseño 100 % a medida</li>
          <li>Entrega en 4–6 sem.</li>
        </ul>
      </article>
    </div>

    <p class="pricing__note reveal">
      ¿Quieres algo más grande o <span class="hand">especial</span>?
      <a href="#contact">Hablamos</a>.
    </p>
  </div>
</section>

<style is:global>
  .pricing {
    padding: var(--section-py) 0;
    background: var(--color-kraft-alt);
    border-top: 1px solid rgba(139, 99, 64, 0.15);
    border-bottom: 1px solid rgba(139, 99, 64, 0.15);
  }
  .price-tags {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2.5rem;
    max-width: 1000px;
    margin: 4rem auto 0;
  }
  .tag {
    position: relative;
    padding: 2.5rem 2rem 2.25rem;
    background: var(--color-paper);
    text-align: center;
    transition: transform 0.3s ease;
    clip-path: polygon(
      0% 18px,
      calc(50% - 12px) 18px,
      50% 0,
      calc(50% + 12px) 18px,
      100% 18px,
      100% 100%,
      0% 100%
    );
  }
  .tag:hover { transform: translateY(-4px); }
  .tag::before {
    content: '';
    position: absolute;
    top: 32px;
    left: 50%;
    transform: translateX(-50%);
    width: 12px; height: 12px;
    background: var(--color-kraft-alt);
    border-radius: 50%;
    box-shadow: 0 0 0 1.5px var(--color-brown) inset;
  }
  .tag::after {
    content: '';
    position: absolute;
    top: -30px;
    left: 50%;
    transform: translateX(-50%);
    width: 1.5px; height: 50px;
    background: var(--color-brown);
    opacity: 0.6;
  }
  .tag__label {
    font-family: var(--font-type);
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    color: var(--color-brown);
    margin-top: 2rem;
    margin-bottom: 0.5rem;
  }
  .tag__title {
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 500;
    font-size: 1.6rem;
    color: var(--color-ink);
    margin-bottom: 1.5rem;
  }
  .tag__price {
    font-family: var(--font-hand);
    font-size: 3rem;
    color: var(--color-green-deep);
    line-height: 1;
    margin-bottom: 0.4rem;
  }
  .tag__price-from {
    font-family: var(--font-type);
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-brown);
    margin-bottom: 1.5rem;
  }
  .tag__divider {
    width: 50px;
    height: 1px;
    background: var(--color-brown);
    margin: 1.25rem auto;
    opacity: 0.4;
  }
  .tag__list {
    list-style: none;
    padding: 0; margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.55rem;
    font-size: 0.92rem;
    color: var(--color-ink-soft);
    text-align: left;
    max-width: 200px;
    margin: 0 auto;
  }
  .tag__list li {
    display: flex;
    align-items: baseline;
    gap: 0.6rem;
  }
  .tag__list li::before {
    content: '✓';
    font-family: var(--font-hand);
    color: var(--color-green);
    font-size: 1.2rem;
    line-height: 0.8;
    flex-shrink: 0;
  }
  .pricing__note {
    text-align: center;
    margin-top: 4rem;
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 400;
    font-size: 1.4rem;
    color: var(--color-ink);
  }
  .pricing__note a {
    color: var(--color-green);
    border-bottom: 1px solid currentColor;
    padding-bottom: 2px;
    transition: color 0.2s ease;
  }
  .pricing__note a:hover { color: var(--color-green-deep); }
  .pricing__note .hand {
    font-family: var(--font-hand);
    font-size: 1.8rem;
    color: var(--color-brown);
    display: inline-block;
    transform: rotate(-2deg);
    margin: 0 0.2rem;
  }
  @media (max-width: 820px) {
    .price-tags { grid-template-columns: 1fr; gap: 2rem; max-width: 360px; }
  }
</style>
