<section class="contact" id="contact">
  <div class="container contact__inner">
    <span class="eyebrow">Hablemos</span>
    <h2 class="contact__title">¿Te animas con <em>una pieza</em>?</h2>
    <p class="contact__lede">
      Sin formularios complicados —<br/>
      escríbeme y hablamos.
    </p>

    <a href="https://wa.me/34600000000" class="contact__primary reveal">
      <span class="contact__primary-icon">W</span>
      Escríbeme por WhatsApp
    </a>

    <div class="contact__or">
      <span>o si lo prefieres</span>
    </div>

    <div class="contact__secondary">
      <a href="mailto:hola@crisvitral.com" class="contact__link">
        <span class="contact__link-label">Email</span>
        <span class="contact__link-value">hola@crisvitral.com</span>
      </a>
      <a href="https://instagram.com/crisvitral" class="contact__link">
        <span class="contact__link-label">Instagram</span>
        <span class="contact__link-value">@crisvitral</span>
      </a>
    </div>
  </div>
</section>

<style is:global>
  .contact {
    padding: var(--section-py) 0;
    background: var(--color-kraft-alt);
    border-top: 1px solid rgba(139, 99, 64, 0.15);
    text-align: center;
  }
  .contact__inner { max-width: 640px; margin: 0 auto; }
  .contact__title {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: clamp(2.25rem, 4.5vw, 3.5rem);
    line-height: 1.15;
    margin-bottom: 1.25rem;
  }
  .contact__title em { font-style: italic; color: var(--color-green); }
  .contact__lede {
    font-family: var(--font-display);
    font-style: italic;
    font-size: 1.25rem;
    color: var(--color-ink-soft);
    margin-bottom: 3.5rem;
    line-height: 1.55;
  }
  .contact__primary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.85rem;
    padding: 1.25rem 2.5rem;
    background: var(--color-whatsapp);
    color: white;
    font-family: var(--font-body);
    font-weight: 700;
    font-size: 1.05rem;
    letter-spacing: 0.02em;
    box-shadow: 0 14px 30px -12px rgba(37, 211, 102, 0.6);
    transition: all 0.25s ease;
  }
  .contact__primary:hover {
    background: var(--color-whatsapp-deep);
    transform: translateY(-2px);
    box-shadow: 0 18px 36px -12px rgba(37, 211, 102, 0.7);
  }
  .contact__primary-icon {
    width: 22px; height: 22px;
    background: white;
    border-radius: 50%;
    color: var(--color-whatsapp);
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 900;
    font-size: 0.78rem;
    font-family: var(--font-body);
  }
  .contact__or {
    margin: 2.25rem 0 1.75rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    font-family: var(--font-hand);
    font-size: 1.2rem;
    color: var(--color-brown);
  }
  .contact__or::before, .contact__or::after {
    content: '';
    width: 30px;
    height: 1px;
    background: var(--color-brown);
    opacity: 0.4;
  }
  .contact__secondary {
    display: flex;
    gap: 2.5rem;
    justify-content: center;
    flex-wrap: wrap;
  }
  .contact__link {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    gap: 0.3rem;
    transition: color 0.2s ease;
  }
  .contact__link:hover { color: var(--color-green); }
  .contact__link-label {
    font-family: var(--font-type);
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    color: var(--color-brown);
  }
  .contact__link-value {
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 500;
    font-size: 1.15rem;
    color: var(--color-ink);
  }
  .contact__link:hover .contact__link-value { color: var(--color-green); }
</style>
