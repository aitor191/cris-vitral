<section class="about" id="about">
  <div class="container">
    <div class="about__grid">
      <div class="about__photo-wrap reveal">
        <div class="about__photo">
          <div class="about__photo-inner"></div>
          <span class="about__photo-label">[ retrato_cris.jpg ]</span>
          <div class="about__photo-caption">— en el taller, una tarde de mayo —</div>
        </div>
      </div>

      <div class="about__content reveal" data-delay="1">
        <span class="eyebrow">Hola</span>
        <h2 class="about__title">Soy Cris.<br/>Llevo <em>quince años</em> cortando cristal.</h2>
        <p class="about__text">
          Empecé en un curso de fin de semana. Iba a ser un hobby
          pero se me fue de las manos: dejé el diseño gráfico, monté
          taller en casa y desde entonces no he parado.
        </p>
        <p class="about__text">
          Trabajo sola, con la técnica Tiffany — tira de cobre, soldadura
          de estaño y pátina. Hago todo el proceso: del boceto a la
          última pasada de cera. Por eso cada pieza tarda lo que tarda.
        </p>
        <p class="about__text">
          No es producción en serie. No hay dos vitrales iguales en mi taller.
        </p>

        <span class="about__signature">— Cris</span>

        <div class="about__stats">
          <div>
            <div class="about__stat-num">15+</div>
            <div class="about__stat-label">Años en el oficio</div>
          </div>
          <div>
            <div class="about__stat-num">200+</div>
            <div class="about__stat-label">Piezas con casa</div>
          </div>
          <div>
            <div class="about__stat-num">1</div>
            <div class="about__stat-label">Par de manos</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style is:global>
  .about { padding: var(--section-py) 0; }
  .about__grid {
    display: grid;
    grid-template-columns: 5fr 7fr;
    gap: 5rem;
    align-items: center;
  }
  .about__photo-wrap {
    position: relative;
    padding: 2rem 0 4rem 2rem;
  }
  .about__photo {
    aspect-ratio: 4 / 5;
    background:
      repeating-linear-gradient(135deg,
        var(--color-kraft-alt) 0,
        var(--color-kraft-alt) 10px,
        var(--color-kraft-deep) 10px,
        var(--color-kraft-deep) 20px);
    padding: 16px 16px 70px;
    background-clip: content-box;
    background-color: var(--color-paper);
    box-shadow:
      0 1px 0 rgba(58, 46, 26, 0.04),
      0 24px 40px -18px rgba(58, 46, 26, 0.28);
    transform: rotate(-2deg);
    position: relative;
  }
  .about__photo-inner {
    width: 100%;
    height: calc(100% - 70px);
    background:
      radial-gradient(ellipse 80% 60% at 50% 30%, rgba(200, 146, 58, 0.15), transparent 60%),
      radial-gradient(ellipse 60% 40% at 50% 80%, rgba(59, 110, 31, 0.12), transparent 60%),
      repeating-linear-gradient(135deg,
        rgba(139, 99, 64, 0.08) 0,
        rgba(139, 99, 64, 0.08) 10px,
        transparent 10px,
        transparent 20px),
      var(--color-kraft-deep);
    position: relative;
  }
  .about__photo-label {
    position: absolute;
    bottom: 90px;
    left: 50%;
    transform: translateX(-50%);
    font-family: var(--font-type);
    font-size: 0.7rem;
    color: var(--color-brown);
    background: rgba(251, 248, 241, 0.9);
    padding: 0.35rem 0.6rem;
    letter-spacing: 0.05em;
  }
  .about__photo-caption {
    position: absolute;
    bottom: 16px;
    left: 16px;
    right: 16px;
    text-align: center;
    font-family: var(--font-hand);
    font-size: 1.25rem;
    color: var(--color-ink);
    line-height: 1.1;
  }
  .about__photo-wrap::before {
    content: '';
    position: absolute;
    top: 0;
    left: 40%;
    width: 120px;
    height: 30px;
    background: rgba(43, 74, 122, 0.4);
    transform: rotate(-12deg);
    border-left: 1px dashed rgba(43, 74, 122, 0.6);
    border-right: 1px dashed rgba(43, 74, 122, 0.6);
    z-index: 2;
  }
  .about__photo-wrap::after {
    content: '';
    position: absolute;
    bottom: 12px;
    right: 8%;
    width: 90px;
    height: 26px;
    background: rgba(200, 146, 58, 0.4);
    transform: rotate(8deg);
    border-left: 1px dashed rgba(200, 146, 58, 0.6);
    border-right: 1px dashed rgba(200, 146, 58, 0.6);
    z-index: 2;
  }
  .about__title {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: clamp(2rem, 3.8vw, 2.8rem);
    line-height: 1.2;
    margin-bottom: 1.5rem;
  }
  .about__title em { font-style: italic; color: var(--color-green); }
  .about__text {
    font-size: 1.05rem;
    line-height: 1.8;
    color: var(--color-ink-soft);
    margin-bottom: 1.25rem;
    max-width: 540px;
  }
  .about__signature {
    font-family: var(--font-hand);
    font-size: 2rem;
    color: var(--color-green-deep);
    margin-top: 2rem;
    transform: rotate(-2deg);
    display: inline-block;
  }
  .about__stats {
    display: flex;
    gap: 3rem;
    margin-top: 2.5rem;
    padding-top: 2rem;
    border-top: 1px dashed rgba(139, 99, 64, 0.3);
  }
  .about__stat-num {
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 500;
    font-size: 2.5rem;
    color: var(--color-green-deep);
    line-height: 1;
    margin-bottom: 0.4rem;
  }
  .about__stat-label {
    font-family: var(--font-type);
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-brown);
  }
  @media (max-width: 900px) {
    .about__grid { grid-template-columns: 1fr; gap: 2rem; }
    .about__photo-wrap { padding: 2rem 0 3rem 0; max-width: 360px; margin: 0 auto; }
    .about__stats { gap: 2rem; flex-wrap: wrap; }
  }
</style>
